@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage<Umbraco.Cms.Core.Models.Blocks.BlockListItem>;
@using Umbraco.Cms.Core.Models
@using ContentModels = Umbraco.Cms.Web.Common.PublishedModels;
@{
    var content = (ContentModels.TeamMember)Model.Content;
}

<div class="flex flex-col border border-[var(--color-border-gray)]">
    @if (content.TeamMemberImage != null)
    {
        <img src="@content.TeamMemberImage.Url()" alt="@content.TeamMemberName"/>
    }
    <div class="py-5 px-6">
        <h4 class="text-xl font-semibold">@content.TeamMemberName</h4>
        <p class="text-[var(--color-notel)]">@content.TeamMemberRole</p>
    </div>
</div>